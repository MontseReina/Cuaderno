# Copiar a .env y rellenar con los datos del proyecto de Supabase (Project Settings → API).
# Si se dejan vacíos, la app arranca en MODO DEMO: guarda todo solo en este dispositivo.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
